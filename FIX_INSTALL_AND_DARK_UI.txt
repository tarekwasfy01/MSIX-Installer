INSTALLATION FIX
- Removes an existing package with the same Identity Name for the current
  Windows user before installing the selected package.
- Fixes the common same-version/different-content deployment error 0x80073CFB.
- Does not use Get-AppxPackage -AllUsers.
- Does not perform administrator-only package removal.

DARK UI
- Dark window background and dark title bar.
- Neutral gray drop area and border.
- Choose File is centered horizontally and vertically.
- Only the Install button is blue.
- The final build remains a one-file EXE.
